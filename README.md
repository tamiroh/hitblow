# hitblow

A simple terminal version of Hit and Blow.

```sh
cargo run
```

Guess the hidden sequence of four distinct digits. The first digit may be `0`.
A digit in the correct position is a hit; a correct digit in a different position is a blow.
Enter `q` to quit.
